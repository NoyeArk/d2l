import torch.nn as nn
from model import Model


def train(net, train_features, train_labels, test_features, test_labels, num_epochs, )


if __name__ == "__main__":

	model = Model()
	loss = nn.MSELoss()
	optim = nn.Adam(lr=0.001)

